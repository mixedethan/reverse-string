# Reverse String (ARM Assembly)

This repository contains an ARM assembly program that performs a reversal of an inputted string.

## How It Works
- The program takes in a string.
- It calculates then reverses and outputs the string.

